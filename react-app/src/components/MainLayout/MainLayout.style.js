import styled from 'styled-components';
import { Layout } from 'antd';

export const PrimaryLayout = styled(Layout).attrs({
  className: 'primary-layout',
})`

`;

export const RightContent = styled(Layout).attrs({
  className: 'right-layout',
})`
  margin-left: 240px;
`;

